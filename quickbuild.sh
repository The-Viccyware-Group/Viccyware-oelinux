#!/usr/bin/env bash

# This script is only used if we just need a quick build done for our dev bots.
# Please actually build properly if you're not a Viccyware developer.

./build/build.sh -bt dev -v 6 
